networks:
  default:
    external: true
    name: docker
services:
  service:
    extra_hosts:
      - host.docker.internal:host-gateway
    image: ghcr.io/maxexcloo/caddy
    labels:
      caddy.tls.dns: cloudflare ${cloudflare_api_token}
      caddy.tls.resolvers: 1.1.1.1
    ports:
      - 80:80
      - 443:443
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - data:/data
volumes:
  data:
